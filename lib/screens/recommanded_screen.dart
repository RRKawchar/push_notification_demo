import 'package:flutter/material.dart';

class RecommandedScreen extends StatelessWidget {
  const RecommandedScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
         child: Text("Recommanded Screen"),
      ),
    );
  }
}
